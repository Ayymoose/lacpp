#pragma once

#include "Testable.h"
#include "Sprite.h"

#include <map>
#include <string>

namespace Tests
{
    /*
    class SpriteTests : public TestUnit
    {
    public:
        constexpr SpriteTests() = default;

        using SpriteTestsMap = const std::map<std::string, void (SpriteTests::*)(void)>;
        void runTests() override;

    private:
        SpriteTestsMap m_tests =
        {
            {"sprite tests", &SpriteTests::spriteTests}
        };
        void spriteTests() noexcept;
    };*/

};